export default class Parser {
  parse(code: string) {
    // const parser = new Parser(code, opt);
    // parser.parse();
    // return {
    //   root: parser.root,
    //   errors: parser.errors,
    // };
  }
}
